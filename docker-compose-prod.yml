version: "3"

services:
  python:
    environment:
      - DJANGO_ENV=production
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=zolaris
      - POSTGRES_PORT=5432
      - DEBUG_ENV=False

  db:
    expose:
      - 5432
    environment:
      POSTGRES_PASSWORD: 'postgres'
      POSTGRES_USER: 'postgres'
      POSTGRES_DB: 'zolaris'
      POSTGRES_PORT: 5432